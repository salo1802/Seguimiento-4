/*class Rect extends Shape{
    constructor(posX, posY){
        super(posX, posY);
        this.posX = posX;
        this.posY = posY;
    }

    draw(){
        fill(0);
        rect(posX, posY, size, size);
        fill(255);
        text(value, posX, posY);
    }
}*/